#ifndef STRINGLIB
#define STRINGLIB

#include <iostream>
#include <string>

using namespace std;

class StringLib
{
public:
    string reverseString(const string &str);
    string toUpperCase(const string &str);
};

#endif /* STRINGLIB */